/**
 * FICHIER DE COMPATIBILITÉ
 * Ce fichier réexporte tout depuis server/db/ (modules thématiques).
 * Le code source réel est dans server/db/*.ts
 * Ce fichier existe pour maintenir la compatibilité avec les imports existants.
 */

export * from './db/index';
